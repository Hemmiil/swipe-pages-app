# Google Apps Script ウェブアプリのURL（like/unlikeログの送信先）
# google-apps-script/Code.gs をデプロイして得たURLを設定する。
# 未設定の場合、クラウド送信は無効になり localStorage のみで動作する。
#
# ローカル: このファイルを .env にコピーして値を設定する。
# 本番(GitHub Pages): リポジトリの Variables/Secrets に VITE_GAS_URL を登録する。
VITE_GAS_URL=
